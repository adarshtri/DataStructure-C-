#include <iostream>
#include "binarytree.h"

using namespace std;

/*int main()
{
    binarytree *btree = new binarytree();
    btree->insertnode(btree->getroot(),2);
    btree->insertnode(btree->getroot(),1);
    btree->insertnode(btree->getroot(),3);

    btree->postorder(btree->getroot());

    bool iscontinoustree = btree->iscontinous(btree->getroot());

    if(iscontinoustree){
        cout<<"Tree is continous."<<endl;
    }else{
        cout<<"Tree is not continous."<<endl;
    }

    return 0;
}*/
